name: Build and Release
on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            rid: win-x64
          - os: ubuntu-latest
            rid: linux-x64

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0

      - name: Install Velopack CLI
        run: dotnet tool install -g vpk

      - name: Publish
        run: dotnet publish -c Release -r ${{ matrix.rid }} --self-contained -o ./publish -p:AssemblyName=IsolationProtocol

      - name: Pack with Velopack
        shell: bash
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          
          # Для Windows Velopack шукає .exe, для Linux - без розширення
          if [ "${{ matrix.os }}" == "windows-latest" ]; then
            EXE_NAME="IsolationProtocol.exe"
          else
            EXE_NAME="IsolationProtocol"
          fi

          vpk pack --packId "IsolationProtocol" \
                   --packVersion "$VERSION" \
                   --packTitle "Isolation Protocol" \
                   --packDir ./publish \
                   --mainExe "$EXE_NAME"

      - name: Upload Release Assets
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ./releases/*.exe
            ./releases/*.AppImage
            ./releases/*.zip